function b_vec = VecBoxSum(x, y, w, h, W, H)    
    b_vec = zeros(W*H,1);
    b_vec( x+W*(y-1) ) = 1;
    b_vec( x+W*(y-1)+w ) =1;
    b_vec( x+W*(y-1+0)+w) = 1;
    b_vec( x+W*(y-1+h)+w) = 1;
    for i=1:h
        b_vec(x+W*(y-1+i):x+W*(y-1+i)+w ) =1;
    end
end